Due to the .gitignore, the .txt and .jsonl files will not appear in this repo and can be created by running the pipeline.sh script. At a minimum, you'll need to put some training examples in a `training.txt` file in this folder.